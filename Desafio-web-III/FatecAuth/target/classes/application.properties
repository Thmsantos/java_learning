spring.application.name=FatecAuth

# Configura��o do Projeto

# Configura��o do Server:
server.port=3700

# Configura��o do Locale:
spring.web.locale-resolver=fixed
spring.web.locale=pt_BR

# Configura��o do Banco de Dados - mySQL:
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/Fatec?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
spring.datasource.username=root
spring.datasource.password=admin

# Configura��o do JPA:
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=update